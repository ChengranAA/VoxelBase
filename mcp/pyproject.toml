[project]
name = "baremri-mcp"
version = "0.1.0"
description = "MCP server for BareMRI — lets AI agents drive the NIfTI viewer"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "mcp>=1.0.0",
]

[tool.uv]
package = false
